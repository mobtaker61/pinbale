/** دادهٔ ثابت برای دکمهٔ inline؛ باید کوتاه بماند (محدودیت API). */
export const CALLBACK_MATERIALS_AGAIN = 'pinbale:materials:again';

/** باز کردن همان منوی /list (انتخاب موضوع) */
export const CALLBACK_OPEN_FOLDER_LIST = 'pinbale:list:open';

/** انتخاب موضوع از لیست؛ اندیس بعد از دو نقطه (مثلاً pinbale:cf:0) */
export const CALLBACK_FOLDER_PICK_PREFIX = 'pinbale:cf:';
